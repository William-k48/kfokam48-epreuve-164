# ============================================================
# docker compose — variables d'environnement (B4 : pas de secret en clair committé)
# Copier ce fichier en `.env` (ignoré par .gitignore) et adapter si besoin.
# ============================================================

# PostgreSQL
POSTGRES_DB=epreuve
POSTGRES_USER=epreuve
POSTGRES_PASSWORD=epreuve
