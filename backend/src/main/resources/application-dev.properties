# Profil "dev" — utilisé dans docker-compose (SPRING_PROFILES_ACTIVE=dev).
# Hérite de application.properties ; on rend le SQL lisible en développement.
spring.jpa.show-sql=true
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.flywaydb=INFO
